/// this is an interface class, to provide any methods without being able to be inherited
/// because dart doesn't recognize interface class, it is written as an abstract class
abstract class FlyingMonster {
  String fly();
}